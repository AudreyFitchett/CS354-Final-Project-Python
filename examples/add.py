#!/usr/bin/env python3
def add_numbers(a, b):
        return a + b

result = add_numbers(10, 5)
print(f"The sum is: {result}")

# shows off the syntax for one number to the power of another number
# this code is just here as a reminder to do this in the terminal
# to run, python3 to enter interactive editor then type in code
# use to show how _ works as a copy of the last printed number
def power(a):
        return 2 ** a

# imaginary numbers - just because it's kind of neat
# can enter interactive editor with python3, then
# type code below to see what happens: 
# z = 3 + 5j
# z.real
# z.imag