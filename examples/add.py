#!/usr/bin/env python3
def add_numbers(a, b):
        return a + b

result = add_numbers(10, 5)
print(f"The sum is: {result}")

# shows off the syntax for one number to the power of another number
# can also use this to show how _ works, but will need to copy it in terminal 
def power(a):
        return 2 ** a

# imaginary numbers